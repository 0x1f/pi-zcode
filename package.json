{
  "name": "pi-zcode",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "ZCode browser-login Coding Plan providers with read-only diagnostics and browser-assisted Start Plan inference",
  "repository": { "type": "git", "url": "https://github.com/0x1f/pi-zcode.git" },
  "keywords": ["pi-package", "zcode"],
  "engines": { "node": ">=22.19.0" },
  "peerDependencies": {
    "@earendil-works/pi-ai": "0.85.0",
    "@earendil-works/pi-coding-agent": "0.85.0"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": { "optional": true },
    "@earendil-works/pi-coding-agent": { "optional": true }
  },
  "pi": { "extensions": ["./index.ts"] },
  "scripts": { "test": "node --test test.ts" }
}
